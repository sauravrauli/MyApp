import 'package:flutter/material.dart';

class TeamDetailScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('USERS DETAILS')),
      body: Center(
        child: Text('This is where i will contain every team members details'),
      ),
    );
  }
}
